# Starter variables for 1Password Environment: [project-slug] Development
# Add these in 1Password → Environments → [project-slug] Development → Variables
# Real values never go in git — only in the Environment.

APP_ENV=development

# Add new variables below as the project needs them (match .env.example names):
# VAR_NAME=value
