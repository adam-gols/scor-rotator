# Environment variables — documented names and fake values for git.
# Real values: 1Password Environment → mounted .env (see docs/secrets.md)
# Do NOT copy this file to .env.
# Packaged apps do not need these — they use the streams proxy.

# Application
APP_ENV=development

# Optional: use the same public proxy as Release builds (no Airtable token needed)
# SCOR_STREAMS_API_URL=https://scor-rotator-streams.example.workers.dev

# Airtable (token is secret; IDs are project config) — local Load Today without proxy
AIRTABLE_TOKEN=pat_replace_me
AIRTABLE_BASE_ID=appbP3QMzpB7ZaW7W
AIRTABLE_TABLE_ID=tblgMy87FGUjJdd11
AIRTABLE_VIEW_ID=viwWeCzdCv6GmEdcj
